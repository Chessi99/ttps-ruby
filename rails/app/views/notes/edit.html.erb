<h1>Editing Note</h1>

<%= render 'form', note: @note,  url: book_note_path(params[:book_id], @note.id) %>

<%= link_to 'Show', book_notes_path(@note.book_id) %> |
<%= link_to 'Back', book_notes_path(@note.book_id) %>
